{% extends 'basefront.html.twig' %}

{% block title %}Edit Planning{% endblock %}

{% block body %}


    <h1>{{ planning.id }} </h1>
    <h1>{{ planning.dateDepart|date('d-m-y') }} </h1>
    <h1>{{ planning.dateFin|date('d-m-y') }} </h1>
    <h1>{{ planning.Description }} </h1>
    <h1>{{ planning.hotels.first.Nom }} </h1>
    <h1>{{ planning.destinations.first.nom }} </h1>
    <h1>{{ planning.evenements.first.nom }} </h1>
    <h1>{{ planning.prix }}</h1>



{% endblock %}
